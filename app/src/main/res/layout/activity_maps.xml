<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:context=".MapsActivity"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin">

    <fragment
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"/>
    <seguridadinformatica.uni.sedondevives.MagicTextView
        xmlns:qwerjk="http://schemas.android.com/apk/res/seguridadinformatica.uni.sedondevives.MagicTextView"
        android:clickable="true"
        android:textStyle="bold"
        android:layout_marginTop="20px"
        android:layout_marginRight="20px"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Enviar ubicación"
        android:id="@+id/btnEnviar"
        android:layout_alignTop="@+id/map"
        android:layout_alignRight="@+id/map"
        android:onClick="onClickEnviar"
        android:textColor="@color/button_color"

        android:textSize="25dp"


        app:outerShadowColor="@color/button_color_selector"
        app:outerShadowDx="5dp"
        app:outerShadowDy="5dp"
        app:outerShadowRadius="6dp"

         />

</RelativeLayout>


